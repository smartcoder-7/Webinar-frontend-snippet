import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const DropdownIconUntil = styled.span`
    ${tw`absolute pointer-events-none border-solid border-4 border-b-0 border-gray-2`}
    border-left-color: transparent;
    border-right-color: transparent;
    border-top-width: 5px;
    top: 35%;
    right: 20%;
`;

export default DropdownIconUntil;
